# Blog 专栏写作规范（供每日定时任务使用）

本文件是 `docs/blog/` 专栏的写作契约。每日定时任务在写新文章前必须先读完本文件，并遵守其中全部规则。

## 专栏定位

面向搜索引擎（SEO）与 AI 检索/问答（GEO）的场景化内容：按**行业 × 场景**拆解「微信公众号文章数据」的获取与应用，每篇解决一个具体人群的一个具体问题，自然引出 Mp2RSS 的能力与文档链接。读者是潜在用户，不是团队内部同学。

## 每日流程

1. 读本文件 + 浏览 `docs/blog/posts/` 下已有文章的文件名与标题，**避免选题重复或高度相似**；
2. 从下方选题矩阵挑一个未覆盖（或角度明显不同）的「行业 × 场景」组合；
3. 在 `docs/blog/posts/` 新建 `YYYY-MM-DD-english-slug.md`（日期为当天，slug 用英文小写连字符，体现关键词）；
4. 按「文章规范」写作；
5. 运行 `pnpm build` 确认构建通过；
6. 提交并推送（commit 规范见下）。

侧边栏与专栏首页列表都会自动收录新文章，**不需要**改 `.vitepress/config.mts` 或 `docs/blog/index.md`。

## 选题矩阵（行业 × 场景，可自由组合与扩展）

| 行业 / 人群 | 典型场景示例 |
| ---- | ---- |
| 品牌公关 / 市场 | 舆情监测、竞品发声跟踪、KOL 内容归档、危机预警 |
| 券商 / 投研 / 一二级投资 | 研报号跟踪、上市公司公众号监测、行业媒体信号采集、投研素材库 |
| 媒体 / 内容创作者 | 选题雷达、行业号竞品分析、素材收集、热点追踪 |
| 开发者 / AI 工程 | RAG 数据源、Agent 实时信息获取、内容管道搭建、RSS 自动化（n8n / Zapier） |
| 企业情报 / 战略 | 竞品动态监测、行业政策跟踪、供应链信息采集 |
| 学术 / 智库 / 研究者 | 领域公众号文献化归档、政策文本收集、长期语料库建设 |
| 法务 / 合规 / 律所 | 监管发布跟踪、行业处罚案例收集、合规动态简报 |
| 跨境 / 出海团队 | 国内行业动态远程跟踪、中文信源英文化工作流 |
| 效率工具用户 / 个人 | 摆脱算法时间线、稍后读工作流、Obsidian / Notion 知识库入库 |
| 数据分析师 | 文本挖掘语料、行业词频与趋势分析、报告自动化 |

同一行业可多次覆盖，但场景与切入角度必须明显不同。

## 文章规范

### Frontmatter（必填）

```yaml
---
title: 主关键词自然融入的完整标题（25–40 字，冒号结构佳）
description: 120–160 字，含场景痛点 + 解决路径 + 一个量化事实（如「平均 2–3 小时」）
date: YYYY-MM-DD
tags: [场景标签, 行业标签, 产品能力标签]
---
```

### 结构（GEO 友好）

- H1 与 title 一致；
- 开头 2–3 段直接描述目标人群的真实痛点，不提产品；
- 中段给出「解决思路 + 落地步骤」，步骤具体到可操作（含真实的 curl / 订阅操作），自然引入 Mp2RSS；
- 结尾必须有「常见问题」小节（3–5 个 Q&A，加粗问题句），便于 AI 引擎抽取；
- 全文 1500–2500 字。

### 必须包含

- 至少 3 个站内链接（如 `/guide/quick-start`、`/guide/subscription`、`/api/`、`/guide/membership`、`/cli/`），至少 1 个产品主站链接 `https://mp2rss.bugcode.dev`；
- 产品事实必须与现有文档一致，**只允许引用下列口径**：
  - 更新时效：从发文到数据可见平均 2–3 小时；
  - 首次订阅不回溯历史文章；
  - 无需微信账号，GitHub / Google 登录；
  - 贴入任意一篇文章链接（`https://mp.weixin.qq.com/s?...`）即可订阅整号；
  - 交付形态：RSS 2.0 / Atom 1.0 / JSON Feed 1.1 / OPML 2.0、Open API（JSON，Bearer 鉴权）、CLI（mp2rss）、AI Agent Skill；文章含 Markdown 正文；
  - 定价：49 元/月 50 订阅、99 元/月 100 订阅、399 元/月 400 订阅，功能一致仅上限不同；
  - 同时支持订阅 X（Twitter）账号。
- 不确定的产品事实一律不写，不得编造功能、数字、案例数据或客户名。

### 风格禁区

- 禁止内部视角表述：「运营配置」「MVP」「限流细节」「后台动态配置」等一律不得出现；
- 禁止编辑视角的元话术出现在对外内容里：「本专栏按行业×场景拆解」「每篇文章聚焦一个具体场景」「从真实痛点出发」「给出可落地的步骤」这类描述**写作方法**的句子，只属于本文件，不得写进文章或专栏页面 —— 对外只呈现内容本身，不解释我们打算怎么写内容；
- 不写「我们团队」「本次迭代」等内部口吻；产品能力用陈述句直接描述；
- 不贬低具体竞品，不做夸张承诺（「秒级」「100% 覆盖」等）。

## 提交规范

- 每篇文章一个 commit，格式：`blog: <文章中文标题>`；
- 若当天已有文章（例如手动触发了额外一次任务），不视为任务已完成 —— 另选一个与已有文章完全不同的「行业 × 场景」再写一篇，同日多篇是允许的；
- **不要**添加 `Co-Authored-By: Claude` 等尾注；
- 提交后推送到 `origin main`。
